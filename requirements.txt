# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Data & config
pydantic>=2.6
pydantic-settings>=2.2

# Image processing
Pillow>=10.2
numpy>=1.26
opencv-python-headless>=4.9

# OCR engine (3.x API, PP-OCRv6 pipeline, polygon detection + recognition).
# Requires PaddleOCR >= 3.0 for the `predict()` / OCRResult dict API.
paddleocr>=3.0
# paddlepaddle is a separate, heavy install. Install matching to your platform:
#   CPU:  pip install paddlepaddle>=2.6
#   GPU:  pip install paddlepaddle-gpu>=2.6  (see https://www.paddlepaddle.org.cn/install/quick)
paddlepaddle>=2.6

# Barcode / QR
pyzbar>=0.1.9
# Requires the zbar system library:
#   macOS:  brew install zbar
#   Debian: apt-get install libzbar0

# VLM fallback (art text). Installed lazily; provider swappable via config.
httpx>=0.27
openai>=1.30  # Qwen-VL / many providers speak OpenAI-compatible API

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
