"""AI Native agent package — ReAct loop with function-calling tools.

qwen3-max (reasoning brain) orchestrates VLM/OCR tools to understand an image.
The brain never sees pixels; it inspects the image through tool outputs.
"""
